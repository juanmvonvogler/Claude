# Orchestrator Agent

Placeholder. Bridges the capability layer and the private context system. At session start it fetches the relevant personal context and hands it to other agents via the context-brief protocol; capability agents never reach into the private system directly.
