# Web Dev Agent — soul/

This agent's identity layer — who the web dev agent *is* as a character. Identity here overlays the shared `foundation/` baseline.

**What belongs here:** the web dev agent's voice, principles, and decision-making style — shaped around coding-partner and teaching dynamics.

**What does NOT belong here:** universals that would apply to every agent (those live in top-level `foundation/`), and any personal context.
